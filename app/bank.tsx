import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function BankRoute() {
  return <ScreenPlaceholder title="Vocabulary Bank" body="Browse, search and edit vocabulary lands in the next phase. The local schema and study loop are already ready for it." />;
}
